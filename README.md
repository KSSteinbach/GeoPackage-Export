# GeoPackage-Export
GeoPackage Export – QGIS plugin: Saves selected vector layers of a QGIS project as GeoPackage files and transfers the styles (colors, symbology) into the layer_styles table of the GeoPackage so they load automatically when the file is reopened.
